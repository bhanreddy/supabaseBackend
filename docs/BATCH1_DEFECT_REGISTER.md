# Batch 1 defect register — audit recorded before fixes, 2026-09-05

Baseline: backend main 7586fd4; frontend main 4d9497e. All initial modifications are the uncommitted Batch 1 files enumerated by git status. No staged changes. No code modified during Stage A.

| ID | Severity | Verified problem / evidence | Impact and minimum fix |
|---|---|---|---|
| B01 | Blocker | feeRecoveryService overview WHERE excludes paid rows; every recovery query omits activeStructureFilter and some omit fs.deleted_at. feePaymentService:remaining and feesRoutes:student summary use amount_due-discount-amount_paid and active mode. | Incorrect efficiency and demands for inactive/deleted structures. Reuse existing mode filter and ledger balances throughout; include settled rows in totals. |
| B02 | Blocker | automationActionService dispatch accepts amountDue/studentName without querying student_fees; processor trusts payload. | A paid fee or foreign-tenant fee can trigger a stale/wrong reminder. Resolve tenant-owned state again and lock financial rows around dispatch. |
| B03 | Blocker | Automated key uses fee+stage; manual key uses student+hour. cooldown_days is never read; no shared student claim. | Concurrent manual/scheduled or multiple fee reminders create duplicate/spam effects. Serialize student claims and enforce persisted cooldown across stages. |
| C01 | Critical | recovery GET routes require only fees.view; schema.sql role seeds and 20260707 portal migration grant it to student/parent. | Children/parents can view other families' debt/contact/history. Use existing staff-role plus permission middleware. Scope every related join, including parent contacts, to school. |
| H01 | High | feeRecoveryRoutes destructures custom_message but reads customMessage in both preview and dispatch. | Preview throws; actual send returns per-student errors without sending. Validate and correctly bind body fields. |
| H02 | High | Worker checks rule only at scan start; no feature or school recheck in dispatch; force scan bypasses rule. | Disabled automation continues sending queued/scanned work. Enforce current school, nav.fees feature and scheduled rule before action; remove force side-effect bypass. |
| H03 | High | dispatch marks no recipients and all provider failures completed, catches errors; claimed failed keys cannot retry. | False delivery metrics, silent loss; unknown provider outcomes unsafe to retry. Use existing tracked notification sender, distinguish skipped/failed/sent, retry only pre-delivery failures. |
| H04 | High | feeAutomationJobService reads whole school, serially sends, has no per-school catch; transportJobService swallows worker exception and skips startup when transport+diary off. | One corrupt tenant aborts others, pg-boss sees success, fee jobs depend on unrelated toggles. Bound scan, isolate failures, propagate retryable errors, independent fee jobs config. |
| H05 | High | FeeRecoveryView ignores pagination; load errors become zero KPIs/clear-account UI; overlapping fetches race; selected IDs persist across filters; errors in send results ignored. | Hidden defaulters, misleading finance UI and wrong bulk selections. Paginate, debounce, discard stale responses, clear selection, display errors and gate actions. |
| H06 | High | tests/feeRecovery.integration.js imports default db and enables school 1 automation with no environment guard. | Running tests can modify configured production and potentially send notifications. Replace with isolated, fixture-backed PostgreSQL test suite and stub only provider boundary. |
| M01 | Medium | overviewCache is an unbounded process-local map invalidated only by two payment paths. | Refunds, adjustments, assignments and other instances show stale metrics. Remove this small cache and unused payment hooks; all reads become fresh. |
| M02 | Medium | segmentation labels repeat for 8–30 days without looking at repeated obligations. | Misclassifies first-time late payer. Base repeat on multiple overdue obligations and expose age-based persistent rationale. |
| M03 | Medium | Generic middleware/audit.js inserts no school_id and silently catches errors; new routes have no explicit business audit. | Sensitive reminder/config actions lack reliable tenant audit. Persist scoped business events, transactionally with rule changes and accepted dispatch intent. |
| M04 | Medium | Date stage uses process-local midnight; SQL uses session CURRENT_DATE; due date idempotency string slices Date.toString. | UTC/IST boundary mismatch and inconsistent execution keys. Normalize fee DATE and use explicit established Asia/Kolkata calendar. |
| I01 | Info | Original migration tables already exist in configured DB, no tracking row; pg_policies empty with RLS enabled. | Preserve deployed SQL. Corrective migration must close optional permissive policy path; internal tables should have no portal grants. |
| I02 | Info | Index names collide with 20260418 partial student_fees index and descending transaction index. | IF NOT EXISTS preserves different existing definitions. Do not rebuild equivalent indexes; add only query-backed history/student claim index. |
| P01 | Blocker, pre-existing | Real PostgreSQL clean deploy: migration 001 succeeds; 002:14 fails relation schema_meta does not exist. No initial baseline in migration chain. | Pure-migrations fresh install is not supported. Cannot reconstruct historic whole-app baseline within Batch 1; document release prerequisite and test Batch 1 on schema-derived fixtures. |
| P02 | High, pre-existing | run_all_migrations --bootstrap-live marks every migration except five historical names applied, including new Batch 1, and treats any unique error as completed file. | Can falsely record unexecuted migrations. Provide explicit Batch 1 fail-closed runner and reject unsafe bootstrap for new files. |

Baseline execution: 111 backend tests pass; 225 Jest tests (+6 Node tests) pass, but npm test exits 1 because pre-existing transportLiveSimulation.test.ts uses node:test and Jest reports an empty suite; tsc passes; Expo web export passes. Lint: 19 errors, 343 warnings, all 19 error files untouched by Batch 1. Accounts HEAD lint independently has one unused Platform warning. Backend has no configured lint/typecheck/build scripts. Existing integration test was inspected, not run against configured school 1. Configured DB inspection was READ ONLY, schema/index metadata only. No notifications sent.

Change map: migration -> automation tables/indexes/RLS; services/automation* -> event/rule/claim/action; feeAutomationJobService + transportJobService -> pg-boss lifecycle; feeRecoveryService -> financial analytics/history/cache; feePaymentService -> payment event/cache hooks; feeRecoveryRoutes + feesRoutes -> API/RBAC; frontend service/component/accounts tab -> real API UI; two tests -> original shallow checks and unsafe integration. No Phase 4+ files targeted.


Stage C additional verified finding H07 (High, fixed): PostgreSQL numeric(12,2) accepts NaN, and the existing nonnegative/overpayment checks evaluate true for NaN. Local SQL reproduced all three checks as true. The original recovery query includes such a balance; Number converts it to NaN and JSON serializes it as null, while reminder text can contain ₹NaN. Added finite-value validation at analytics, preview and dispatch boundaries. Corrupted candidate raises a visible data error; cross-school scans continue. A real constrained-table regression inserts this record and verifies no outbound notification.

Stage C compatibility finding V01 (Blocker, concurrent work; fixed): newly added transportSafetyService imported nonexistent publishAutomationEvent, breaking existing transportPhaseE tests and the server route import graph. Changed only that import to alias the existing emitSchoolEvent. No transport behavior or event handlers implemented in this audit. Separately, concurrent attendance imports of removed generic action helpers and new notification-category mappings were repaired by the concurrent work before the passing Batch 1 integration rerun. These later-phase files remain unaudited.
